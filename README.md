# 🛠️ 格式化工具 (Format Tools)

> JSON 格式化 • Properties ⟷ YAML 轉換 • 圖片轉 Base64 • OCR 文字辨識

這是一個基於純前端的格式化工具，提供 JSON 格式化、Properties/YAML 雙向轉換、圖片轉 Base64、OCR 文字辨識功能。

---

## 📁 專案結構

```
json-yaml-converter/
├── index.html      # 主頁面 HTML 結構
├── app.js          # 核心邏輯 (FormatTools 類別)
├── styles.css      # 樣式表 (深色主題)
├── libs/           # 本地 JavaScript 函式庫
│   ├── js-yaml.min.js      # YAML 解析與生成 (v4.1.0)
│   └── tesseract.min.js    # OCR 文字辨識 (v5)
└── README.md       # 專案文檔
```

---

## ✨ 功能總覽

### 1️⃣ JSON 格式化區塊

| 功能 | 說明 |
|------|------|
| **格式化** | 將未格式化的 JSON 轉換為縮排 2 空格的美化格式 |
| **即時驗證** | 輸入內容時自動驗證 JSON 語法正確性 |
| **錯誤定位** | 顯示錯誤行號並高亮標示問題位置 |
| **折疊/展開** | 支援對 `{` 和 `[` 的內容進行折疊 |
| **檔案上傳** | 支援上傳 `.json` 檔案 |

### 2️⃣ Properties ⟷ YAML 轉換區塊

| 功能 | 說明 |
|------|------|
| **Properties → YAML** | 將 Properties 格式轉換為 YAML 格式 |
| **YAML → Properties** | 將 YAML 格式轉換為 Properties 格式 |
| **巢狀結構支援** | `server.port=8080` 會轉換為 YAML 的巢狀結構 |
| **多行文字處理** | 正確處理 YAML `|` block scalar 與 Properties 的轉換 |

### 3️⃣ 圖片轉 Base64 區塊

| 功能 | 說明 |
|------|------|
| **拖放上傳** | 支援拖放圖片到指定區域 |
| **多選上傳** | 可同時上傳多張圖片 |
| **圖片預覽** | 顯示圖片縮圖與檔案資訊 |
| **複製 Base64** | 單張複製或一鍵複製全部 |

### 4️⃣ OCR 文字辨識區塊

| 功能 | 說明 |
|------|------|
| **多語言支援** | 正體中文、簡體中文、英文及混合模式 |
| **拖放上傳** | 支援拖放圖片進行辨識 |
| **進度顯示** | 即時顯示辨識進度 |
| **複製文字** | 單張複製或一鍵複製全部辨識結果 |

> ⚠️ OCR 首次使用需下載語言模型（約 10-15MB/語言），請耐心等待。
>
> **⚠️ 辨識限制與已知問題：**
> - OCR 準確度受圖片清晰度、字體大小、背景複雜度影響極大。
> - 對於手寫字體、特殊藝術字體或過於潦草的文字辨識率較低。
> - 雖然系統內建了深色背景自動反轉功能，但在極端對比度或複雜背景下仍可能辨識錯誤。
> - 若遇到英文或數字辨識錯誤，請嘗試切換「正體中文 + English」模式。

---

## 🏗️ 技術架構

### 外部依賴

| 資源 | 來源 | 說明 |
|------|------|------|
| **js-yaml** | 本地 (`libs/js-yaml.min.js`) | YAML 解析與生成 (v4.1.0) |
| **Tesseract.js** | 本地 (`libs/tesseract.min.js`) | OCR 文字辨識 (v5) |
| **Google Fonts** | CDN | Inter + JetBrains Mono 字體 |

### 核心類別 `FormatTools`

位於 `app.js`，使用 ES6 Class 語法。主要功能分類：

- **JSON 處理**：`formatJson()`, `validateJson()`, `toggleFold()`
- **Properties/YAML**：`propsToYaml()`, `yamlToProps()`, `parseProperties()`
- **圖片 Base64**：`handleBase64Upload()`, `fileToBase64()`, `renderBase64Previews()`
- **OCR 辨識**：`handleOcrUpload()`, `recognizeText()`, `renderOcrPreviews()`

---

## 🚀 如何使用

1. 直接用瀏覽器開啟 `index.html` 即可使用
2. 無需安裝任何相依套件
3. 支援現代瀏覽器 (Chrome, Firefox, Edge, Safari)

---

## 📅 更新日誌

| 日期 | 版本 | 說明 |
|------|------|------|
| 2025-12-14 | v1.1.0 | 新增圖片轉 Base64、OCR 文字辨識功能 |
| 2025-12-14 | v1.0.0 | 初始版本，JSON 格式化與 Properties ⟷ YAML 轉換 |


